package services;

import services.Response.Response;

/**
 * This class is used to be the Fill service for the server.
 */


public class Fill {

    /**
     * Generates (numGen) generations worth of persons and associated events for those people.
     * If the user already has data associated, it should be deleted and replaced.
     * if numGen is not specified or is negative, the default should be 4.
     * @param userName Username of who needs information in the server
     * @param numGen Number of generations to create for the username inserted
     * @return a response object of success or failure
     */
    private Response fill(String userName, int numGen){

        return null;
    }
}
